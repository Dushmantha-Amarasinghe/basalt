{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the Basalt Host window is allowed to do. Short on purpose: everything that touches the drive, the network or the device list goes through this app's own commands, so none of Tauri's filesystem or HTTP plugins are needed. The only plugin here is the folder picker, which exists so a user can share a folder rather than a whole drive.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-minimize",
    "core:window:allow-maximize",
    "core:window:allow-unmaximize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "core:window:allow-is-maximized",
    "core:event:default",
    "dialog:allow-open",
    "opener:allow-open-path"
  ]
}
