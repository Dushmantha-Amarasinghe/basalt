/target
**/*.rs.bk
*.pdb

# Benchmark artifacts — corpora are gigabytes, results are regenerated
/bench-corpus/
/bench-out/
/docs/benchmarks.json

# Node / Tauri (phases 1+)
node_modules/
dist/
.vite/

# Editor / OS
.DS_Store
Thumbs.db
.idea/

# Built binary staged for copying to the laptop
/dist/

# Test media — never commit multi-GB files
*.mkv
*.mp4
*.avi

# Tauri shell build output (kept out of the workspace target dir)
/target-tauri/
/target-probe/

# Tauri shells: build output, generated schemas, and the libmpv drop
apps/*/src-tauri/target/
apps/*/src-tauri/gen/schemas/
apps/*/src-tauri/lib/
